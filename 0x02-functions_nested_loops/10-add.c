#include "main.h"

/**
 * add - Write a function that adds two integers and returns the result
 * Return: always 0
 * @a: an integer
 * @b: second integer
 */
int add(int a, int b)
{
	return (a + b);
}
