#include "main.h"

/**
 * islower - Write a function that checks for lowercase character
 * Return: 1 if lowercase otherwise 0
 */

int _islower(int c)
{
	int i = 'a';

	for (i = 'a'; i <= 'z'; i++)
		if (i == 'c')
		{
		return (1);
		}
		else
		{
		return (0);
		}
}
